<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Best of Yeti</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">Yeti Tracker</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
{#                    <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Best of Yeti</a>#}
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_yeti_new') }}">Add New Yeti</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('yeti_random') }}">Yetinder</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('yeti_statistics') }}">Stats/Admin</a>
                </li>
            </ul>
        </div>
    </nav>
</header>

<main role="main" class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <h1>Top 10 Best Rated Yeti</h1>
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>#</th>
                    <th>Name</th>
                    <th>Height</th>
                    <th>Weight</th>
                    <th>Location</th>
                    <th>Rating</th>
                </tr>
                </thead>
                <tbody>
                {% for yeti in bestYetis %}
                    <tr>
                        <th scope="row">{{ loop.index }}</th>
                        <td>{{ yeti.name }}</td>
                        <td>{{ yeti.height }}</td>
                        <td>{{ yeti.weight }}</td>
                        <td>{{ yeti.location }}</td>
                        <td>{{ yeti.rating }}</td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="6" class="text-center">No Yetis found</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</main>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
